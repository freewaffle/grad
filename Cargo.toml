[package]
name = "grad"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "s"
lto = "thin"
strip = true

# ---------------------------------------------------------------

[dependencies]
